import React from "react";
import { Text, View } from "react-native";

const Proxy = () => {
    return(
        <View>
            <Text>This is the proxy page</Text>
        </View>
    )
}

export default Proxy;